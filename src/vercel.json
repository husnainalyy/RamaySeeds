{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "dist/**",
        "installCommand": "npm install --include=dev"
      }
    }
  ]
}